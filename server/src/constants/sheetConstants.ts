export const sheetName = "snookerplus";

export const usernameColIndex = 4;
export const pinColIndex = 5;

export const playerCredentialsRange =`${sheetName}!${String.fromCharCode(64 + usernameColIndex)}:${String.fromCharCode(64 + pinColIndex)}`